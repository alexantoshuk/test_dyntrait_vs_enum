[package]
name = "test_dyntrait_vs_enum"
version = "0.1.0"
edition = "2024"

[dependencies]


[dev-dependencies]
criterion = { version = "^0.5", features = ["html_reports"] }


[profile.bench]
# debug = true
opt-level = 3
lto = true
panic = "abort"
codegen-units = 1

[[bench]]
name = "bench"
harness = false
